# =============================================================================
# requirements.txt
# Pin to minor versions for reproducible builds.
# Use `pip install -r requirements.txt` to install.
# =============================================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1        # ASGI server with websocket + uvloop support

# --- Database ---
sqlalchemy==2.0.30               # Core ORM (2.x has native async support)
pymysql==1.1.1                   # Pure-Python MySQL driver (no C deps)
cryptography==42.0.8             # Required by PyMySQL for SSL/caching_sha2

# --- Caching & Distributed Locks ---
redis==5.0.4                     # redis-py (includes async client)

# --- Message Broker & Task Queue ---
celery[rabbitmq]==5.4.0          # Distributed task queue
kombu==5.3.7                     # Celery's messaging library (AMQP)

# --- Settings & Validation ---
pydantic==2.7.2
pydantic-settings==2.3.1         # .env file loading via pydantic
email-validator==2.1.1           # Required by pydantic EmailStr

# --- Utilities ---
python-dotenv==1.0.1             # Load .env in development
PyJWT==2.9.0                     # JWT token signing and verification

# --- Development / Testing (optional — remove in slim prod image) ---
httpx==0.27.0                    # async HTTP client for TestClient
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
